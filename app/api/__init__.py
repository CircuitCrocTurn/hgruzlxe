"""HTTP API для дашборда.

Эти эндпоинты вызывает фронт ``/dashboard``, ``/responses``, ``/chats``,
``/settings`` через ``fetch()`` для сохранения настроек, запуска авто-
откликов, подключения площадок и т.п.

Часть эндпоинтов сейчас намеренно живёт как заглушка — они принимают
данные, фиксируют изменения на ``users`` / :class:`PlatformCredential`,
но ничего не отправляют наружу. Когда появится реальная интеграция
с воркерами / hh.ru / SMTP — содержимое функций расширится, контракт
фронта менять не придётся.
"""
